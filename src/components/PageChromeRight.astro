---
/**
 * PageChromeRight — PC 右カラム chrome（SP では l-shell 側で非表示）。
 * 季節 LP では「補助ナビ / LINE 登録 / CTA」などを置くことが多い。
 * 案件ごとにこのファイルの中身を実装する。slot を渡せば差し込みも可能。
 */
---
<div class="c-page-chrome c-page-chrome--right">
  <slot>
    {/* TODO: 右カラムの内容（補助ナビ / LINE 登録 / CTA など）をここに実装 */}
  </slot>
</div>
