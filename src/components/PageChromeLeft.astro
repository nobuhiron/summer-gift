---
/**
 * PageChromeLeft — PC 左カラム chrome（SP では l-shell 側で非表示）。
 * 季節 LP では「ロゴ / ページタイトル」などを置くことが多い。
 * 案件ごとにこのファイルの中身を実装する。slot を渡せば差し込みも可能。
 */
---
<div class="c-page-chrome c-page-chrome--left">
  <slot>
    {/* TODO: 左カラムの内容（ロゴ / ページタイトル など）をここに実装 */}
  </slot>
</div>
